import { Fab } from 'senanginvoice'

// Fixed-position floating action button — render in a frame.
export const Default = () => (
  <div style={{ position: 'relative', height: 160, width: 320, background: '#f9fafb', borderRadius: 12, overflow: 'hidden' }}>
    <Fab />
  </div>
)
